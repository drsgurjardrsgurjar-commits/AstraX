name: astrax
description: DMAstraX Flutter app
publish_to: 'none'
version: 0.0.1+1
environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  firebase_storage: ^11.0.0
  google_sign_in: ^6.0.0
  flutter_webrtc: ^0.9.0
  video_player: ^2.5.0
  flutter_dotenv: ^5.0.2
  http: ^0.13.6
  uuid: ^3.0.7
  google_generative_ai: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
