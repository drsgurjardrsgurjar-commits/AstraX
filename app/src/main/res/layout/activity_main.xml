<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black_bg">

    <!-- Screen Content Container -->
    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation">

        <!-- 1. Home Screen (Always Visible by Default) -->
        <include
            android:id="@+id/layout_home_screen"
            layout="@layout/fragment_home"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="visible" />

        <!-- 2. Tools Hub Viewer -->
        <LinearLayout
            android:id="@+id/layout_tools_hub"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/black_bg"
            android:padding="16dp"
            android:visibility="gone">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp">

                <TextView
                    android:id="@+id/tools_category_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_white"
                    android:textSize="20sp"
                    android:textStyle="bold" />

                <Button
                    android:id="@+id/btn_back_to_home"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_alignParentEnd="true"
                    android:text="← Home"
                    android:textSize="12sp"
                    android:backgroundTint="#2A1B4E" />
            </RelativeLayout>

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <TextView
                    android:id="@+id/tools_list_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="#D1D5DB"
                    android:textSize="15sp"
                    android:lineSpacingExtra="8dp" />
            </ScrollView>
        </LinearLayout>

        <!-- 3. Simple Title for Other Tabs -->
        <TextView
            android:id="@+id/other_screen_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/text_white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Bottom 5 Navigation Tabs -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_alignParentBottom="true"
        android:background="@color/card_bg"
        app:itemIconTint="@color/text_white"
        app:itemTextColor="@color/text_white"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

    <!-- Floating Panic Switch -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_panic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_above="@id/bottom_navigation"
        android:layout_margin="16dp"
        android:alpha="0.6"
        android:src="@drawable/ic_panic"
        app:backgroundTint="#FF4757"
        app:tint="@color/text_white"
        app:fabSize="mini" />

    <!-- Emergency NCERT Study Notes -->
    <LinearLayout
        android:id="@+id/emergency_study_layer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#FFFFFF"
        android:padding="20dp"
        android:visibility="gone">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Class 12 Physics - Quick Notes"
                android:textColor="#000000"
                android:textSize="18sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btn_close_emergency"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_alignParentEnd="true"
                android:text="Back"
                android:textSize="12sp"
                android:backgroundTint="#2C3E50" />
        </RelativeLayout>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Chapter 4: Moving Charges and Magnetism&#10;&#10;1. Biot-Savart Law:&#10;dB = (mu0 / 4pi) * (I dl sin theta) / r2&#10;&#10;2. Ampere Circuital Law:&#10;∮ B . dl = mu0 * I&#10;&#10;3. Lorentz Force:&#10;F = q(E + v x B)&#10;&#10;Chapter 5: Thermodynamics&#10;&#10;1. First Law:&#10;dQ = dU + dW"
                android:textColor="#333333"
                android:textSize="15sp"
                android:lineSpacingExtra="6dp" />
        </ScrollView>
    </LinearLayout>

</RelativeLayout>
